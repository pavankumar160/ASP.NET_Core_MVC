@using System.Text.Json
@model string

@{
    var items = JsonSerializer.Deserialize<Dictionary<string, string>>(Model);
}

<h2>Cart Items</h2>
<ul>
@foreach (var pair in items)
{
    <li>@pair.Key : @pair.Value</li>
}
</ul>
