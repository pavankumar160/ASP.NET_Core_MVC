
<form asp-controller="Home" asp-action="Index2" method="get">
  <button type="submit">Second Get Request</button>
</form>


@model List<string>

@if(Model != null && Model.Count > 0){
	foreach(var product in Model)
	{
		<h2>@product</h2>
	}
}